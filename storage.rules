rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Upload e download passano esclusivamente dall'API FileX Cloud.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
