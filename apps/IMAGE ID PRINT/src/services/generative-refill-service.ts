export async function tryGenerativeRefillEdges(
  source: HTMLCanvasElement,
  _intensity: number,
): Promise<HTMLCanvasElement> {
  // Placeholder for optional local integration (ComfyUI / Fooocus / custom local API).
  // By default this feature is intentionally non-destructive and disabled unless configured.
  void source
  throw new Error('Generative refill non configurato localmente')
}
